$(function () {
    // var keyword = $('.search>.key');
    // $('.keys>a').click(function () {
    //     $(this).attr('href', $(this).attr('href')+keyword.val());
    // });
    // location.pathname.search(/\/search\/(\w+)\/([^\/]+)\/?/);
    // var title = RegExp.$1, key = decodeURI(RegExp.$2);
    // $('.keys>a[title="'+title+'"]').css('color', 'rgba(245, 92, 164, 0.73)');
    // keyword.val(key);
    // $('div.search>a').click(function () {
    //     if($(this).attr('class')=='re')
    //         $(this).attr('href', $(this).attr('href')+$(this).prev().val());
    //     else
    //         $(this).attr('href', '/search/'+title+'/'+keyword.val());
    // });
    // $('.search>.key').keydown(function (event) {
    //     if(event.keyCode==13)
    //         if($(this).attr('class')=='key re')
    //             location.pathname = $(this).next().attr('href')+$(this).val();
    //         else location.pathname = '/search/'+title+'/'+keyword.val();
    // });
});